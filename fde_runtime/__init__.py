# -*- coding: utf-8 -*-
"""fde_runtime — solo FDE 原子化运行时（纯标准库）。

把 solo FDE 能力拆成可独立运行、可被组装的「原子」，用统一运行时 + 组装链 + 自进化
把它们串成端到端闭环（监测→诊断→交付→验收）。

四不变量（对齐生态原子化架构 + CodeAgent 范式）：
  A1 单一职责 / A2 自包含 / A3 可独立运行 / A4 可组装
"""
__version__ = "0.1.0"
