# solo-agent-kit — VISION

> 一个给一人公司/中早期创业者的**方法论驱动、轻量、独立 Agent**。
> 定位：不是又一个通用框架，是"轻量化的 Hermes"——把一个人用 Agent 干完整活的方法论，做成最小可跑、脱离任何平台的独立主体；保留成长为完整 Agent 的可能，但以轻量为前提。

版本：0.1 (VISION)
状态：草稿，待审
作者：zhengjinjun1975
许可证：Apache-2.0

---

## 0. 一句话

**solo-agent-kit 是一个独立运行、方法论统一、可成长的轻量 Agent**——你现在用它做一人公司的事（记忆/写作/代码/开源），它不绑定任何平台；将来它长成什么，由你的方法论演进决定，而不是由"框架该有什么功能"决定。

## 1. 为什么存在（问题）

一人公司/中早期的人用 Agent，面临三座大山：

| 痛点 | 现状 | 代价 |
|---|---|---|
| 记忆断层 | 每次会话重新介绍背景，项目上下文靠人脑 | 效率低、重复劳动 |
| 方法论不沉淀 | 踩过的坑、验证过的流程散在聊天记录 | 无法复用、新人上手慢 |
| 绑定平台 | 用某平台 Agent，能力、数据、方法论都被锁死 | 迁移难、被平台绑架 |

现有工具两种极端：通用框架（Hermes/Claude Code）强大但重、学习成本高、绑定生态；单点工具（记忆库/检查器）零散、不成体系。

**solo-agent-kit 站在中间：方法论驱动 + 轻量独立 + 可成长。**

## 2. 核心设计哲学（方法论统一的前提）

设计它的一切决定，都从这四条方法论原则出发，而非"框架该有什么"：

1. **单一事实源（SSoT）** — 同一件事只有一个权威位置。记忆如此（三层两域）、项目上下文如此（场景块）、写入路径如此（唯一决策树）。
2. **本体优先（Ontology-first）** — 先建"实体-关系-属性"语义结构，再谈检索与理解。这是它区别于普通记忆库/工具调用的根本。
3. **反过度工程（Anti-overengineering）** — 一人公司不需要团队治理/权限矩阵/微服务。能单文件解决就不拆框架，能标准库就不引依赖，能 CLI 就不上服务。
4. **诚实定位（Honest positioning）** — 方法论 + 思路，非对标大厂。能力边界写清楚，不夸大。

## 3. 它是什么 / 不是什么

| 是 | 不是 |
|---|---|
| 轻量化的 Hermes | 又一个通用 agent 框架 |
| 方法论驱动的独立主体 | 平台的插件/皮肤 |
| 可成长的起点 | 功能堆砌的成品 |
| 一人公司的干活动手工具 | 团队协作平台 |

**与 Hermes 的关系**：solo-agent-kit 是"Hermes 的方法论精华、去掉平台依赖的轻量版"。它保留 Hermes 的骨架（agent 循环/记忆/工具/skill/网关），但每个都是标准库、独立、可替换的实现。**它现在够用，未来可以长成 Hermes 那样**——但长成什么样，由你的方法论决定，不是照抄 Hermes 的功能清单。

## 4. 方法论如何统一成整体（核心）

所有模块不是"拼一起的工具"，而是**同一套方法论在不同层的落地**：

```
┌─────────────────────────────────────────────────────────┐
│                    solo-agent-kit                        │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ agent.py │  │ cli.py   │  │ gateway  │  │ (future)│ │  ← 入口层
│  │ 循环/决策 │  │ 命令界面  │  │ 消息平台  │  │ agent间 │ │
│  └────┬─────┘  └────┬─────┘  └──────────┘  └─────────┘ │
│       │             │                                   │
│  ┌────▼─────────────▼───────────────────────────────┐   │
│  │              方法论核心 (model-agnostic)           │   │
│  │                                                  │   │
│  │  ontology.py  本体建模 (先建语义结构)               │   │
│  │  memory.py    三层两域记忆 + 语义检索 + 摘要树      │   │
│  │  skill.py     任务提取可复用经验 + 版本/触发边界     │   │
│  │  writing.py   六维写作检查 D1-D6                   │   │
│  │  code.py      代码影响分析 impact()               │   │
│  │  task.py      任务状态控制面                       │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**统一原则：所有模块共享同一套数据模型和写入决策树。** 一条事实进来，走唯一的决策路径：高频关键→memory 画像层；项目定稿→场景块；其他有价值→memory 事实层；一次性→不记。本体层为其他所有层提供语义锚点。

## 5. 模块设计（每块 = 验证过的方法论去框架化）

| 模块 | 能力 | 来源 | 独立实现 |
|---|---|---|---|
| `ontology.py` | CSV→实体-关系-属性本体 + 语义锚点 | factory-ontology | 标准库 |
| `memory.py` | 三层两域 + 语义检索(nomic) + 摘要树 | OptMem | 标准库 + Ollama(可替换) |
| `skill.py` | 从任务提取可复用经验 + 触发边界/版本 | Hermes skill 思路 + ACL2026记忆综述 | 标准库 |
| `writing.py` | 六维写作检查 D1-D6 | zh_writing_checker | 标准库 |
| `code.py` | 代码影响分析 impact() | codegraph | 标准库 |
| `task.py` | 任务状态控制面(断点续跑) | task_state | 标准库 |
| `agent.py` | agent 循环：感知→记忆→选工具→行动→输出 | Hermes 骨架 | 标准库 |
| `cli.py` | `solo run/init/skill-add` JSON in/out | buzz-cli 思想 | 标准库 |

**每块都是单文件、标准库、可独立 import**——这保证"脱离框架"是真脱离，不是名义上。

## 6. 边界（这一版不做）

- 不做团队协作/权限矩阵/ACL（一人公司不需要，反过度工程）
- 不做多 agent 编排（留接口，v2+ 再说）
- 不做 GUI/桌面端（CLI 够用）
- 不做微服务/分布式（单进程，将来需要再加）
- 不做平台绑定（不依赖 Hermes/OpenClaw/任何 agent 框架）

## 6.5 统一推理模型（本地 + 远端混合）

solo 的推理不是单一模型，是**分层混合**，由 `provider.py` 抽象层统一调度（可替换、可配置，不硬编码）：

| 任务类型 | 模型 | 理由 |
|---|---|---|
| 轻量推理（记忆/写作/压缩/标题/摘要） | 本地 Ollama ornith | 快、免费、私有、高频低复杂度 |
| 复杂推理（本体建模/复杂代码/长文） | 远端 DeepSeek | 需强模型，低频高价值 |
| 语义嵌入（记忆检索） | 本地 nomic-embed-text | 记忆私有，不泄露 |

**方法论原则**：
- **模型分层**：简单工作走本地，复杂工作走远端——省 token、保隐私、性能优先
- **provider 可替换**：用户可配任一任务走本地/远端；本地模型可换，远端可换 API
- **降级兜底**：远端不可用时降级本地，本地不可用时明确报错（不静默）
- **隐私边界**：涉及私有数据（记忆/本体/写作）默认走本地，只有明确复杂推理才送远端

## 6.6 记忆存储：独立为主，Obsidian 接口兼容

solo 的记忆是**自包含的**（独立运行不需要 Obsidian），但保留与 Obsidian 的互通接口：

| 层 | 存储 | 说明 |
|---|---|---|
| 热域(画像) | solo 内部 | 每轮注入 |
| 温域(事实+场景) | solo 内部(文件+语义索引) | 独立运行核心 |
| 冷域(归档) | Obsidian(可选) + 文件 | **可选接入** |

**设计**：
- **独立**：memory.py 用自有文件格式 + 语义索引，拉下来就能跑，零 Obsidian 依赖
- **导入器**：`solo import-obsidian` 把已有 Obsidian vault（Markdown）一键导入成 solo 记忆——保留你现有 1889 条资产
- **导出器**：`solo export-markdown` 把 solo 记忆导出成 Markdown，可回写 Obsidian
- **共存**：已用 Obsidian 的人可两套并行（Obsidian 当档案，solo 当活记忆）；新用户可完全不碰 Obsidian

**方法论**：独立性是底线（solo 必须自包含才算独立主体），接口是兼容（Markdown 互通，不锁死）。Obsidian 是"可选冷域档案"，不是依赖。

## 7. 成长路线图（预留空间，方法论演进驱动）

```
v0.1  VISION + 骨架：agent.py + cli.py + memory.py 最小闭环
v0.2  方法论铺满：ontology/writing/code/skill/task 全接入
v0.3  记忆强化：语义检索稳定 + 摘要树自动巩固 + SSoT 全链路
v0.4  独立可用：CLI 跑通"一人公司完整活"(写文/开仓/查记忆)
v1.0  首个开源发布：方法论完整、文档齐全、诚实定位
v2.0  gateway：常驻服务 + 接消息平台(钉钉/微信)
v3.0  agent 间协作 + MCP 生态
     │
     └── 每步由方法论需求驱动，不为了"像 Hermes"而加功能
```

**成长原则**：它从"轻量 CLI"长成什么，取决于你实际用到的需求，不是预埋功能。**先 CLI 立住方法论，再按需长。**

## 8. 诚实定位声明（开源时写进 README）

> solo-agent-kit 是一个给一人公司/中早期创业者的方法论驱动轻量 Agent。它不试图替代 Hermes、Claude Code 等成熟框架——它是"轻量化的 Hermes"：把一个人用 Agent 干完整活的方法论（本体建模→记忆→skill→写作→代码→开源合规）做成最小可跑、脱离平台的独立主体。能力边界清楚，方法论为魂，轻量为本。

**三行"不是什么"**（吸收 Buzz 的 What-it-is-not，比三页"是什么"更建立信任）：
- 不是又一个通用 Agent 框架——它轻量、方法论驱动，非对标大厂
- 不是 AI 替代人类的计划——人留在循环里，agent 是帮手不是取代
- 不是已完成的产品——它诚实标注什么能用、什么在长

## 9. 与 Buzz 精髓的关系

Buzz 的"agent 即成员+独立审计"、"频道即项目记录"、"统一事件日志"三个思想，作为**方法论层**吸收（见 skill.py 的任务提取、memory.py 的 SSoT、task.py 的审计），但**不采用**它的 Rust/多服务架构（反过度工程）。

**深度吸收的 Buzz 可落地设计**（来自 buzz-cli 与 VISION_AGENT）：
- **可审计性标准**：小到能装进脑袋，症状→根因几分钟 trace（§12）
- **JSON + 分级退出码**：agent-first 接口，0-5 退出码区分失败类型（§12.5）
- **工具调用即产出**：核心交付物是动作，文本是推理（§12.5）
- **两协议无耦合**：agent 循环与工具之间走 MCP 协议组合，不写死 import（§15 预留）

**不吸收**：Postgres+Redis+Typesense 多服务、Nostr relay、Rust 多 crate——这些是"重"的另一半，与 solo 轻量冲突。

## 10. 决策记录

| 决策 | 理由 |
|---|---|
| 标准库 Python | 零依赖，任何人能 import，真脱离框架 |
| CLI 首发 | 轻量，方法论立住再长服务 |
| 本体优先 | 差异化锚点，市面上无轻量开源 |
| 单文件模块 | 反过度工程，可替换可成长 |
| Apache-2.0 + NOTICE | 符合开源合规方法论 |

## 11. 目标人群精确定义（"必要工具"的前提）

不是"任何人"，是**有清晰边界的特定人群**：

| 维度 | 定义 |
|---|---|
| 角色 | 一人公司创始人 / 中早期创业者 / 独立开发者 / **工厂级 FDE（开发工程师）** |
| 特征 | 没有团队、没有预算请工程师、但已经在用 AI Agent 干活 |
| 痛点 | 记忆断层、方法论不沉淀、被平台绑定、**个人能力有上限** |
| 场景 | 写内容、管知识、写代码、解决问题、开源发布——一个人干完公司的活 |
| 非目标 | 有团队协作需求的人、要平台生态的人、不愿学命令行的纯业务 |

**FDE/OPC 定位升级（2026-08）**：一人公司（OPC）和工厂级 FDE 是**同一物种的两副面孔**——都是"一个人顶一个团队"。FDE 核心能力 = 写文档 / 写代码 / 解决问题 / 理解代码库，solo 把这些能力做到**轻量、可放大、可复用**。差异化锚点从"一人公司"升级为"**把个人能力放到最大的工具箱**"，且工厂场景用"工厂本体优先"（设备/工单/运维数据）——这是你的 factory-ontology 已验证的差异化。

**"必要工具"的判定**：它解决的是"一人公司不用它就会重复劳动/丢方法论"的问题——不是"锦上添花"，是"干活必需"。

**市场证据（2026-08 Forbes 报道）**：单人公司占新创企业 **1/3+**（2019 年仅 23.7%）；完整 AI 栈年成本 **$3k-12k**，替代人力团队 **$80k-120k/月**；Sam Altman 预测 2026-28 出现十亿美元单人公司。**"Context engineering"（上下文工程）被点名是单人创始人的关键能力**——solo 正是把这个能力产品化。这证明"一人公司 + 方法论 Agent"不是小众实验，是正在发生的趋势。

## 12. 轻量化落地约束（量化标准）

"轻量化"不是形容词，是可验收的量化标准：

| 维度 | 标准 |
|---|---|
| 依赖 | **零第三方 Python 依赖**（纯标准库）；Ollama/DeepSeek 是可选外部服务 |
| 代码 | 核心 < 4000 行（全部模块加起来） |
| 安装 | `pip install` 或 `git clone` 后一条命令跑通，< 1 分钟 |
| 启动 | CLI 冷启动 < 1 秒 |
| 学习 | 读 README 10 分钟内能跑第一个任务 |
| 移除 | 卸载不留残留，全部数据在单一目录 |

**反过度工程红线**：任何"为了像大框架而加"的功能，如果违反以上任一条，不做。

**轻 ≠ 不行（核心命题，竞品对比的教训）**：轻量是壳，不是能力上限。solo 要证明"轻量但能打"，靠三件事：
1. **方法论深度代替代码量**：不是功能少，是每个功能有方法论支撑——本体优先让检索更准（不是"更少检索"），场景块让上下文更全（不是"更少记忆"），可证伪进化让 agent 更强（不是"更简单"）。**轻量的是工程，深度的是方法论。**
2. **可验证的"能打"证据**：每次 `solo run` 留下结果；开源时带 benchmarks/示例证明它能完成一人公司的真实活（写文/开仓/查记忆），而非"轻量但只能 Hello World"。
3. **零依赖但不零能力**：标准库实现的是完整方法论，不是玩具。sqlite-vec 零依赖但向量检索完整，importlib 插件零框架但可扩展——**技术选型轻，能力边界不轻**。

**判定标准**：如果一个竞品能做到"又轻又能打"，说明它用更少代码实现了同能力，solo 该学；如果竞品"轻但只能做琐事"，solo 靠方法论深度拉开。**solo 的轻量对标的是"工程极简"，不是"能力阉割"。**

**可审计性标准（吸收 Buzz：小到能装进脑袋）**：solo 必须"小到能装进脑袋"——任何一个模块失败，能在几分钟内从症状追溯到根因。如果加一个功能导致"无法快速 trace 失败链路"，即使代码行数没超，也违反轻量。**轻量的本质是可审计性，不是行数。**

## 12.5 Agent 接口设计（吸收 buzz-cli：JSON 化 + 分级退出码）

solo 的 CLI/接口是 **agent-first** 设计，对齐 buzz-cli 的方法论：

- **JSON in / JSON out**：所有输入输出是结构化 JSON（agent 可编程调用），人读文本是可选展示层
- **分级退出码**（让自动化能区分失败类型）：
  ```
  0 = 成功
  1 = 用户参数错误
  2 = 网络错误
  3 = 认证/权限错误
  4 = 其他错误
  5 = 写冲突（需人工决策）
  ```
- **工具调用即产出**：agent 的核心交付物是**动作**（写文件/查记忆/调本体/提取skill），文本只是推理过程（可流式展示）。对齐 Buzz "The agent's useful output is its tool calls; text is reasoning."
- **stdin 可读长输入**：`solo run "任务" -` 支持从 stdin 读 body（对齐 buzz-cli 的 `--content - < file`）

## 13. 开源合规声明（开源时写入）

遵循用户成熟的开源方法论（Apache-2.0 + NOTICE + 诚实定位）：

- **LICENSE**：Apache-2.0（标准全文，从 API 拉，不手打）
- **NOTICE**：派生来源声明——每模块若借鉴了别的项目（OptMem/codegraph/zh_writing_checker/factory-ontology/她的开源项目），在 NOTICE 写清楚来源、URL、版权、许可
- **诚实定位**：README 明说"非对标大厂，方法论驱动，轻量，能力边界清楚，不夸大"
- **脱敏**：发布前全库扫密钥/内部路径/雇主名（遵循开源发布脱敏流程）

## 14. 面向用户的 README 骨架（功能化）

README 不是文档堆，是"拉下来 10 分钟跑通"的入口：

```markdown
# solo-agent-kit
> 一人公司/中早期创业者的**本体优先**方法论 Agent —— 记忆/写作/代码/开源，一个人干完整活。
> 轻量，但能打。零依赖，但方法论完整。不是通用 agent，是专为人公司定制的方法论套件。

## 为什么是"本体优先"（不是又一个记忆库/RAG）
- RAG 检索文本，本体检索知识——solo 先建实体-关系语义结构，再检索
- 这不只是更准的搜索，是让 agent 理解"你的领域里什么是真的、什么能行动"
- 这是 solo 区别于全部同类竞品的核心（ibl.ai 证实这是 RAG 的缺失层）

## 它解决什么（痛点表）
## 快速开始（3条命令跑通）
   pip install solo-agent-kit
   solo init                # 初始化记忆库
   solo run "写一篇公众号推文"   # 走完整方法论
## 核心能力（记忆/本体/写作/代码/skill 各一节，带最小示例）
## 配置（本地模型/远端模型/provider.yaml）
## 与 Obsidian 的关系（可选导入导出）
## 限制与诚实声明（不做什么）
## 路线图
## License / 致谢（NOTICE）
```

## 15. 成长为专业智能体的架构预留（成长不变量）

solo 会长大，但**有三个东西永远不变**（这是"轻量 Hermes"不偏离的锚）：

| 不变量 | 说明 |
|---|---|
| 方法论内核不变 | 本体优先 + SSoT + 反过度工程 + 诚实定位——无论加多少功能，这四条是骨架 |
| 单文件标准库模块不变 | 每个新能力仍是单文件、标准库、可独立 import |
| 独立主体不变 | 永远不绑定平台；外部系统只走接口 |

**成长路径（从轻量到专业）：**
```
v0 CLI 单主体（现在）      → 一个人命令行干活的 Agent
v1 gateway 常驻服务         → 接消息平台（钉钉/微信），像 Hermes gateway
v2 多 agent 协作 + MCP      → 方法论套件长成协作系统
v3 harness 自进化           → 三支柱可观察性 + 可证伪进化（§23）
v4 专业智能体               → 特定行业（如本体驱动数据问答）深度定制
     ↓
     每一步仍守三条不变量，否则停止成长做减法
```

**v2 的架构预留（吸收 Buzz 两协议无耦合）**：solo 的 agent 循环与工具之间，从 v1 就保持"可走 MCP 协议"的边界——不写死 import，工具可通过 MCP 组合。这样 v2 接外部工具/MCP 生态时零重构。**现在不实现 MCP，但预留协议边界**，避免将来推翻重来。

## 15.5 身份与审计预留（吸收 Buzz：agent 即成员）

当前 v1 是本地 CLI，不需要身份系统。但 §19 安全边界预留"身份签名"字段：solo 的每次写操作可带签名 + 审计事件（谁、何时、做了什么、影响哪些文件），对齐 Buzz "agent 有自己的密钥和审计轨迹"。**v1 留字段，v2 接身份时启用**——不预埋复杂身份系统，但数据结构上不排斥它。

## 17. 核心 Agent 循环（"Agent 基本形态"的定义）

solo 的 Agent 不是"一堆工具"，是一个**可运行的循环**。这是它作为独立主体的心脏：

```
                    ┌─────────────┐
   用户/CLI 任务 ──→ │  感知 Input  │
                    └──────┬──────┘
                           ▼
                    ┌─────────────┐
                    │ 记忆装载     │  ← memory.py 调出相关上下文(热域+温域检索)
                    │ Context      │
                    └──────┬──────┘
                           ▼
                    ┌─────────────┐
                    │ 推理决策     │  ← provider.py 模型分层: 简单→本地,复杂→远端
                    │ Reason       │
                    └──────┬──────┘
                           ▼
                    ┌─────────────┐
                    │ 行动 Tools   │  ← 本体/写作/代码/skill/task 按需调用
                    └──────┬──────┘
                           ▼
                    ┌─────────────┐
                    │ 记忆写入     │  ← 唯一决策树: 高频→画像/项目→场景/其他→事实
                    │ Commit       │
                    └──────┬──────┘
                           ▼
                    ┌─────────────┐
                    │ 输出 Output  │  ← JSON(agent-first) + 可选人读文本
                    └─────────────┘
```

**循环五态**：感知→记忆装载→推理→行动→记忆提交→输出。每一步可断点（task.py 记录状态），失败可续跑。

**主动思考（吸收 Burin：会话间内省）**：除被动响应任务，solo 预留"会话间主动思考"——在空闲时内省待办、跟进承诺、主动提出值得说的洞察。**这不是 cron 定时任务，是每次的判断调用**（对齐 Burin "not a cron job—a judgment call every time"）。v1 可选，v2 完整实现。这使 solo 从"被动的工具"长成"主动的伙伴"。

## 23. Harness 工程（吸收 AHE 论文：三支柱可观察性 + 可证伪进化）

> 来源：arXiv 2604.25850《Agentic Harness Engineering》（AHE）。这定义了 solo 主体设计的**成长引擎**——不只干活，还能**自动改进自己**。

### 核心洞见
**harness = 模型之外的适应面**（system prompt/tools/middleware/skills/配置/长期记忆）。模型固定时，改 harness 就能显著提升任务完成率。**AHE 实证：增益集中在 tools + middleware + 长期记忆，而非 system prompt**——这印证你的方法论（记忆/场景块/SSoT 正是"长期记忆"层）。

### 三支柱可观察性（solo 主体设计的核心升级）

| 支柱 | AHE 实现 | solo 落地 |
|---|---|---|
| **① 组件可观察性** | harness 解耦为 7 种可编辑组件，每个是文件、可 git 回滚 | solo 的每个模块（ontology/memory/skill/writing/code/task）是独立文件、可版本回滚 |
| **② 经验可观察性** | 原始轨迹蒸馏成分层可下钻的证据语料 | solo 的 memory 摘要树 + skill 提取 = 经验层 |
| **③ 决策可观察性** | 每个编辑配自声明预测，下轮验证——**可证伪契约** | solo 的 task.py 记录每次改动的预期 + 下轮验证 |

### 可证伪进化闭环（solo 的"自我改进"引擎）

```
solo run "任务" → 记录轨迹(trajectory)
      ↓
提炼失败模式 → 定位到单个组件(component observability)
      ↓
编辑组件 + 自声明预测(decision observability)
      ↓
下轮验证 → 预测对则保留,错则回滚(file-level revert)
```

**关键设计原则（对齐你的方法论）**：
- **可证伪预测**：每次 solo 改进自身都带"我预期这会让 X 更好"，下轮验证——这和你记忆里 PenguinHarness 四纪律的"可证伪预测+rubric评测隔离"同源
- **最小种子**：AHE 强调从最小 harness 开始，让每个组件"靠实测赢得位置"——反过度工程
- **文件级回滚**：每个组件独立文件 → git 历史 → 失败可回滚单文件，不整体回退
- **长期记忆是增益点**：AHE 证明记忆层携带最大改进，solo 的三层两域正好是差异化

### 成长预留
v1 是"循环五态"（§17 干活）；**v2 升级为"进化闭环"（§23 自我改进）**。v1 不实现进化，但架构上预留：
- 每个模块独立文件（可单组件编辑回滚）
- task.py 记录决策 + 预期（未来验证用）
- trajectory 记录（未来蒸馏经验）

**方法论**：solo 不只"干活"，还能"改进自己"——这是它从"轻量 Hermes"长成"专业智能体"的引擎。但守反过度工程：v1 只预留结构，不实现进化，等 v2 需要时再加。

## 24. 轻量工程实现（零依赖技术选型，2026-08 全网最优轻量方案）

solo 的每个工程环节都选**最轻量的已验证方案**，不引重依赖：

| 环节 | 轻量方案 | 来源/理由 |
|---|---|---|
| **向量检索** | `sqlite-vec`（零依赖 C 扩展，SQLite 虚拟表）| 单一 C 文件、MIT/Apache双许可、python 可用；对齐标准库原则，替代重向量库 |
| **本地推理** | Ollama + 8GB 量化小模型（Qwen3.5/Gemma4 等）| 8GB VRAM 跑 1B-8B 量化模型，贴近本地-云混合（OpenJarvis 实证）|
| **上下文压缩** | ACON 方法（压缩指南优化 + 蒸馏小压缩器）| 微软 ICML2026：省 26-54% 峰值 token 且保 95% 准确率；可蒸馏进本地小模型 |
| **插件机制** | 单文件插件 + 声明注册（`PLUGINS.md` 或入口扫描）| 不引插件框架，标准库 `importlib` 即可 |
| **分发** | `pip install` + 零第三方依赖声明 | pyproject 零依赖；核心 <4000 行 |
| **多步任务编排** | YAML 工作流（message/reaction/schedule/webhook 触发）| 对齐 buzz-workflow 轻量版，声明式非代码 |

**上下文压缩策略（ACON 启发）**：长上下文任务，solo 用"压缩指南优化"——给定"全上下文成功但压缩失败"的成对轨迹，让模型分析失败原因并更新压缩指南。v1 用简单规则压缩（本地模型），v2 蒸馏专用小压缩器。**省内存且保质量**，符合"轻量不牺牲性能"。

**向量检索落地**：memory.py 用 sqlite-vec 存嵌入向量（`float[N]` 虚拟表），配合 nomic-embed-text 本地嵌入——**全本地、零外部数据库**，数据在单一 SQLite 文件。

**插件机制（v1 预留）**：solo 的核心是标准库，但留插件钩子——用户可写单文件插件扩展工具，声明式注册。**不引框架，importlib 扫描即可**。这支撑 §15 的成长：solo 的能力边界由插件扩展，而非改核心。

**核心数据模型**（所有模块共享）：

| 概念 | 定义 | 归属 |
|---|---|---|
| `Context` | 当前任务的记忆+状态快照 | agent 循环 |
| `Event` | 一次动作的签名记录（审计） | agent 循环 |
| `Memory` | 三层两域持久化 | memory.py |
| `Ontology` | 实体-关系-属性语义锚点 | ontology.py |
| `Skill` | 可复用经验的封装(版本/触发/步骤) | skill.py |

**统一原则**：Context 是唯一的"当前工作集"，Event 是唯一的"发生了什么"，两者共同驱动记忆写入——保证"同一件事只有一个权威位置"（SSoT）。

## 18. 模块与已验证组件的映射（"融合全部方法论"可追溯）

每个 solo 模块必须能追溯到已验证的实现，保证方法论是真融合，不是重写：

| solo 模块 | 借鉴源 | 融合的方法论 |
|---|---|---|
| `ontology.py` | factory-ontology 套件 | 本体建模→检索命中率提升 |
| `memory.py` | OptMem | 三层两域 + 摘要树 + SSoT |
| `skill.py` | Hermes skill 体系 | 从任务提取可复用经验 |
| `writing.py` | zh_writing_checker | 六维写作检查 D1-D6 |
| `code.py` | codegraph | 改代码前影响分析 |
| `task.py` | task_state | 断点续跑、状态外置 |
| `provider.py` | 模型分层配置 | 简单→本地/复杂→远端 |
| `agent.py` | Hermes 骨架(轻量化) | Agent 循环五态 |

**约束**：每模块是"方法论的去框架化复刻"——保留方法论精华，去掉平台依赖，标准库实现。

## 19. 安全边界（自动化 vs 需确认）

轻量不等于无脑自动化。明确安全边界：

| 操作 | 策略 |
|---|---|
| 记忆读写/本体建模/写作检查/代码分析 | 自动 |
| 写文件/创建仓库/推送代码/发消息 | **需确认**（CLI `--yes` 才跳过）|
| 删除/覆盖不可逆操作 | **强制确认** |
| 外部网络请求 | 白名单制 |

**原则**：读自动，写需确认，删必确认。对齐"反过度工程 + 诚实边界"方法论。

**MCP 安全预留（2026-08 云安全联盟 v1，若 v2 走 MCP 必须遵守）**：
- **最小权限**：scope 定义到工具级而非服务级——能读文件的工具不该带删文件的权限
- **工具描述完整性**：工具定义视为版本化不可变资产；每次会话比对工具描述是否变更（防 rug-pull/静默篡改）
- **沙箱隔离**：MCP server 默认无文件系统/无外网访问，只对白名单路径和端点放行
- **签名响应**：工具输出签名，防中间人篡改影响模型推理
- **供应链验证**：MCP server 版本固定（如依赖 pinning），警惕恶意包（2025-09 npm 供应链攻击波及 MCP SDK）
- **租户隔离**：记忆/向量库在存储层强制命名空间分割，不止查询层
- **JIT 权限提升**：高权限操作需实时再授权，不靠会话级常驻权限

**方法论**：solo 若成长到 v2 MCP，安全不是后置——从 v1 的工具设计就预留"最小权限+审计"字段。这支撑 §15.5 身份签名预留的实战意义。

## 20. 验收/测试策略（轻量但完整）

| 层 | 方式 |
|---|---|
| 每模块 | 冒烟测试（标准库 unittest，验证核心路径）|
| Agent 循环 | 端到端：`solo run "任务"` 走通五态 |
| 回归 | 每加功能跑全量 smoke |
| 语义检索 | 用固定语料验证召回质量（不依赖外部数据）|

**原则**：测试保"轻量且可靠"，不追求覆盖率数字，保核心路径不破。对齐用户"测试先冒烟后全面，失败修到过"的工作风格。

## 21. 方法论来源（证据锚定，2026-08 全网检索）

solo 的每条方法论不是拍脑袋，有业界验证来源。这些既支撑设计，也是 README 的证据背书：

| 来源 | 核心主张 | solo 对应 |
|---|---|---|
| **Burin**（burin.ai，"Personal AGI for one person"）| 特定框架优于通用框架；学到新东西就写 skill（代码不是笔记）；会话间主动思考 | 支撑"为一人公司定制"定位 + skill.py + §17 agent主动思考 |
| **OpenJarvis 论文**（arXiv 2605.17172）| 本地模型在个人任务接近云（差3.2pp）但成本低 ~800×；本地-云混合是可行架构 | §6.5 本地+远端混合的证据 |
| **记忆调查综述**（arXiv 2605.06716）| 记忆演进三阶段：Storage→Reflection→Experience | 三层两域可对齐此演进框架 |
| **ibl.ai Ontology vs RAG**| RAG 检索文本，本体检索知识；agent 行动需要结构而非相似文本 | 本体优先差异化锚点的权威论证 |
| **Openclad**（foodlbs，一人 24/7 agent）| 风险评估分级（低风险自动/高风险需确认）；调度 skill；SQLite+向量记忆轻量栈 | §19 安全边界 + 轻量存储佐证 |
| **Forbes 报道**（2026-08，"AI agent 成为新单人创始人剧本"）| 单人公司占新创 1/3+；AI栈年$3k-12k vs 团队$80k-120k/月；"context engineering"是关键 | §11 定位市场证据 |
| **云安全联盟**（Agentic MCP Security v1）| MCP 最小权限/工具完整性/沙箱隔离/签名响应/供应链验证 | §19 MCP 安全预留 |
| **AHE**（arXiv 2604.25850，Agentic Harness Engineering）| harness 三支柱可观察性 + 可证伪进化闭环；增益集中在 tools/middleware/长期记忆 | §23 主体成长引擎 |
| **sqlite-vec**（asg017）| 零依赖 SQLite 向量搜索，单一 C 文件 | §24 轻量向量检索 |
| **ACON**（微软，ICML2026）| 上下文压缩省 26-54% token 且保 95% 准确率 | §24 上下文压缩策略 |

**方法论结论**：solo 的"特定优于通用 + 本体优先 + 本地远端混合 + 轻量"四大主张，均有业界验证来源背书——不是个人偏好，是可辩护的方法论。

## 25. 竞品定位与差异化（2026-08 全社区对比）

solo 不回避竞争——诚实对比，明确生存空间：

### 竞品图谱
| 竞品 | ★ | 定位 | 本质 |
|---|---|---|---|
| mem0 | 62.9k | 通用记忆层 | 记忆 API，给其他 agent 用，非独立 agent |
| deer-flow | 79.6k | 长 horizon harness | agent 框架，重编排 |
| nanobot | 46.8k | 超轻量个人 AI | 通用 agent，快但无本体/方法论 |
| khoj | 36.4k | AI 第二大脑 | 检索问答，重文档 RAG |
| zeroclaw | 32.5k | 小且自主个人助手 | 通用 agent |

### 差异化（3 个真实、可辩护的点）
| 维度 | 竞品普遍 | solo 独有 |
|---|---|---|
| **本体优先** | 无一做本体建模（全是文档检索/通用记忆）| 先建实体-关系本体再检索——ibl.ai 证明这是 RAG 缺失层，但轻量开源无人做 |
| **方法论完整** | mem0只做记忆、deer-flow只做harness、nanobot只做agent | 记忆+本体+skill+写作+代码+开源一套闭环，不是一个功能 |
| **一人公司定位** | 全是"通用个人助手" | 只服务一人公司，Forbes 证实的趋势定位 |

### 竞争威胁（诚实）
- **nanobot 最危险**：已"超轻量个人AI"。solo 若只做"轻量agent"会被碾压，必须靠"本体优先"拉开
- **mem0 抢记忆心智**：solo 记忆优势在"三层两域+场景块"方法论，非记忆功能本身
- **deer-flow 抢 harness 心智**：solo 差异在"可证伪进化+轻量"，非 harness 概念

### 生存空间结论
solo 差异化成立，但**只在"本体优先 + 方法论完整 + 一人公司"三点的交叉点**。单看任一点（轻量/记忆/harness）都有更强竞品；但没有一个是"本体优先的、方法论完整、专为一人公司"的。**守住这个交叉点 = 独特；滑向"通用轻量agent" = 输给 nanobot。**

## 26. 竞品反查的可优化点（从对比中提炼）

竞品对比不只是定位，还反查出 solo 可提升处：

| 竞品优点 | solo 可吸收 | 是否破坏轻量 |
|---|---|---|
| nanobot 的"超轻量"体验 | 冷启动极快、单命令可用——solo 应把"拉下来即用"做到极致 | 否，本就是目标 |
| khoj 的多源检索 | solo 的 memory 应支持多种数据源导入（不仅是 Obsidian）| 否，走接口兼容 |
| mem0 的记忆 API 化 | solo 的 memory 可暴露为"轻量记忆 API"，供其他程序调用 | 否，标准库即可 |
| deer-flow 的 harness | 已吸收（§23 AHE）| 否 |
| zeroclaw 的自主性 | 强化 §17 主动思考（会话间内省）| 否 |

**优化原则**：吸收竞品优点，但每个都过"轻量红线"——不引重依赖、不破坏零依赖。吸收的是"设计思想"（多源/API化/自主），不是"功能堆砌"。

## 22. 记忆演进理论（吸收 ACL2026 综述：Storage→Reflection→Experience）

来自 arXiv 2605.06716（ACL 2026 Findings）。这为 solo 的 memory/skill 提供**教科书级理论基础**：

### 记忆三阶段（solo 对应）
| 阶段 | 含义 | solo 对应 |
|---|---|---|
| **Storage** 存储 | 保留原始轨迹 | memory 冷域(会话留底) |
| **Reflection** 反思 | 提炼并注回，辅助相似任务 | memory 摘要树 + nap 巩固 |
| **Experience** 经验 | 跨轨迹抽象成通用规则 | **skill.py**（最高层）|

### 跨轨迹抽象三层粒度（skill.py 成长路径，最关键）
| 粒度 | 形式 | solo 落地 |
|---|---|---|
| 浅层 | 自然语言规则 | 首版 skill：踩坑/流程写成规则文本 |
| 中层 | 模块化可执行骨架 | v2：skill 是代码骨架（对齐 Burin"代码即记忆"）|
| 深层 | 模型权重内化 | v3+：经验固化进模型（预留，不预埋）|

**设计含义**：skill.py 从"自然语言规则"(浅层)起步，长到"代码骨架"(中层)——正好对齐 Burin"学到新东西就写 skill，代码不是笔记"。**三层粒度给 skill.py 明确的成长路线**，不预埋复杂，但路径清晰。

### 主动探索（吸收 Experience 阶段的 proactive exploration）
记忆不只是"存进去等检索"，要**主动探索未知**：空闲时主动检索记忆、发现缺口、补全知识。这与 §17 的"主动思考"呼应——solo 的记忆层预留主动探索钩子（v2）。

### 三个驱动（为什么需要演进）
长程一致性 / 动态环境挑战 / 持续学习——solo 的三层两域天然满足：热域保证一致性，温域适应动态，skill 沉淀持续学习。

## 16. 工程落地必需项（完整化的骨架）

VISION 之外，工程必须有这些才算"完整落地"（每个都是独立交付物）：

| 文件 | 内容 |
|---|---|
| `LICENSE` | Apache-2.0 全文 |
| `NOTICE` | 派生来源声明 |
| `pyproject.toml` | 打包元数据（零依赖声明）|
| `solo/` | 核心包（§5 + §17 模块）|
| `cli.py` | 命令入口 |
| `provider.yaml` | 模型配置模板（本地/远端分层）|
| `workflows/` | YAML 工作流定义（§24 编排）|
| `plugins/` | 单文件插件目录（§24 预留）|
| `examples/` | 一人公司真实场景（写文/查记忆/开仓）|
| `tests/` | 每模块冒烟测试（§20 策略）|
| `docs/VISION.md` | 本文件（方法论）|
| `docs/ARCHITECTURE.md` | 实现架构（当 VISION 定稿后）|
| `README.md` | §14 功能化入口 |
